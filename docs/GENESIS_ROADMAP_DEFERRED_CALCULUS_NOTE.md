# Genesis Engine — Deferred calculus/operator-system note

This note is the active sequencing rule for the inherited repository line after the rollback from the premature annotation/encoder branch.

## Active sequencing rule

The Hilbert-space calculus / anchor-actuation / calculus-operator-system work is explicitly **deferred to last priority**.

That means:
- do **not** use it to justify premature CPU annotation/encoder rollout,
- do **not** lock a schema for it yet,
- do **not** expand it into parallel operator paths while other roadmap structure remains unfinished.

## Required continuation order

Until the repository has no remaining non-calculus structural roadmap work, continuation should prefer:
1. residual canonical cleanup on the active line,
2. remaining editor/runtime/export/apply wording cleanup,
3. remaining bounded single-source-of-truth cleanup,
4. only then the calculus/operator-system lane.

## Stop condition

When the active inherited line reaches the point where the only meaningful remaining roadmap work is the calculus/operator-system lane, stop and surface that state to the user instead of improvising a schema.

## Constraint reminder

The calculus/operator-system lane must remain one canonical path when it is eventually addressed:
- no aliases,
- no compatibility shims,
- no duplicate source-of-truth logic,
- no premature encoder ownership claims for CPU surfaces.
